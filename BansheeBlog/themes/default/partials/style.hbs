<style>
    html, body {

        --primary-font-color: #212121;
        --tint-color: #448AFF;
        --link-color: #1A237E;
        background-color: #fafafa;
        width: 100%;
        height: 100%;
        margin: 0;
        padding: 0;
    }
    body {
        color: var(--primary-font-color);
        font-family: 'Tajawal', sans-serif;
        border-top: 4px solid var(--tint-color);
    }

    a.no-link-style {
        text-decoration: none;
        color: var(--link-color);
    }

    a.no-link-style:hover {
        color: var(--tint-color);
    }

    header {
        text-align: center;
        font-family: 'Black Han Sans', sans-serif;
        margin-top: 50px;
        margin-bottom: 50px;
    }

    nav {
        margin: 15px;
        float: left;
        width: 25%;
        border-right: 2px solid var(--tint-color);
        font-family: 'Black Han Sans', sans-serif;
    }

    nav li {
        list-style: none;
        text-align: right;
        margin-right: 15px;
    }

    nav a.no-link-style, header a.no-link-style {
        color: var(--primary-font-color);
    }

    .article-tags {
        float: left;
        width: 30%;
    }

    .truncate {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    main {
        float: left;
        width: 66%;
    }

    article {
        margin-bottom: 30px;
    }
    article .post-title {
        font-size: 24pt;
        margin-bottom: 0;
    }

    article .post-time {
        list-style-type: circle;
        margin-bottom: 10px;
    }

    section, section img {
        max-width: 100%;
    }

    section code {
        white-space: pre-wrap;
        word-break: break-all;
    }

    section img {
        max-width: 100%;
    }
    section.post-content-short p {
        margin-bottom: 0;
    }
    a.post-content-short {
        float: right;
        margin-top: -5px;
    }

    footer {
        text-align: center;
        float: left;
    }

    @media only screen and (max-width: 640px) {

        nav, main, .article-tags {
            width: 100%;
        }
     
        nav ul, nav li {
            margin-bottom: 0;
        }

        nav li, .article-tags li {
            display: inline-block;
            text-align: center;
            margin-right: 15px;
        }
    }
</style>